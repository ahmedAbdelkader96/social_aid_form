// Centralized API endpoint constants for the application.
// Keep paths here to avoid hardcoding strings across repos.
export const API_ENDPOINTS = {
  APPLICATIONS: '/applications',
  COUNTRIES: '/countries',
}

export default API_ENDPOINTS
